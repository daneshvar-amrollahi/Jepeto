func f(x, y): {
    if (x - 2 or 3 * 7 is 10):
        if (b - 2 > 10):
            if (c or 5):
                return a;
            else: {
                print("haG molayi shod");
                return "molayi1";
            }
        else:
            if (x - y * 10 < (a - 2) / 2):
                return b;
            else:
                return "molayi2";
    else: {
        if (72 - a < "str"):
            return a;
        return b;
    }
}

main: print(a + 7 + (
                    "molz" - 5 * f(
                                    g(molof)
                                   )
                    )
            );

func first(x):
    return "bye";

func sec():
    return "hi";