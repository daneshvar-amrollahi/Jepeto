main : Main(c = [1, 3, []], b = 3 is 5, a = 3, d = r);


func  Main(a, b, c, d) :{
    f (c = ---a);
    if (a - --b / --c * (a[b[a[--a]]])) : {

      return ((a * b ):: a * b /c).size;

      print(a * b / (c - 2));


    }
    else :
        if (3 - 7 is -4) :
            return "A" + [33, 53, [false][true]["b"]].size;

  return b[c and d or 1::c / -(-1::c)];
  return 2 - "b"::d[a and 3] + r()(4);

}

func f(c):
  return r;

func r():
  return f;

