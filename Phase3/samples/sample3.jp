 main :  f(b = "str", a = true);

func f(a, b) : {
    g(a, b);
    if (a) :
        return void;
    else :
        return a and b;
}

func g(arg1, arg2) : {
    print(f(arg1 + arg2 * arg1, "str2"));
    return arg1;
    h(arg2 - arg1, []::arg1);
}

func h(a, b) : {
    return f(b - a, a + b * 54 + ["str"]);
}

func noVisit(a, b) : {
    return 1 + true;
}